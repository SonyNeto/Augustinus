{
  "name": "augustinus-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start:frontend": "bun run --filter=augustinus-frontend dev",
    "start:cli": "bun run --filter=@augustinus/cli start",
    "start:api": "bun run --filter=@augustinus/api start",
    "format": "prettier --write ."
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.9",
    "prettier": "3.8.3",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "separador-silabas": "^2.4.1",
    "vite": "^7.1.9"
  }
}
